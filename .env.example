# =============================================================================
# CONFIGURACION DEL BACKEND - Sistema de Transporte
# =============================================================================

# Entorno
NODE_ENV=development

# Puerto del servidor
PORT=3001

# Base de datos PostgreSQL
DATABASE_URL="postgresql://usuario:password@localhost:5432/transporte_db?schema=public"

# JWT
JWT_SECRET=tu_clave_secreta_muy_segura_cambiar_en_produccion
JWT_EXPIRES_IN=4h

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173

# API Peru (apiperu.dev) - Consultas DNI/RUC
# Obtener token en: https://apiperu.dev (plan gratuito: 100 consultas/mes)
APIPERU_TOKEN=tu_token_de_apiperu

# =============================================================================
# TIMEZONE - Configuracion de zona horaria
# =============================================================================
# IMPORTANTE: El backend debe operar en UTC para consistencia.
# En Railway esto se configura en railway.toml.
# En desarrollo local, configurar esta variable:
TZ=UTC

# =============================================================================
# WASABI S3 - Almacenamiento en la nube
# =============================================================================
# Configuracion para almacenamiento de archivos en Wasabi S3
# En desarrollo local, estas variables son opcionales (usa almacenamiento local)
# En produccion (Railway), son OBLIGATORIAS

WASABI_ACCESS_KEY_ID=tu_access_key_id
WASABI_SECRET_ACCESS_KEY=tu_secret_access_key
WASABI_ENDPOINT=https://s3.us-east-1.wasabisys.com
WASABI_REGION=us-east-1
WASABI_UPLOADS_BUCKET=transporte-herminio-uploads
WASABI_BACKUPS_BUCKET=transporte-herminio-backups

# =============================================================================
# NOTAS:
# - Copiar este archivo como .env
# - Cambiar los valores segun el entorno
# - NUNCA subir el archivo .env al repositorio
# - TZ=UTC es OBLIGATORIO para consistencia temporal
# - Las variables WASABI_* son opcionales en desarrollo local
# =============================================================================
